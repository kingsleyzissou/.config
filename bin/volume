#!/usr/bin/bash

INCREMENT=5

audio() {
  canberra-gtk-play -i audio-volume-change -d "changevolume"
}

down() {
  pamixer -d $INCREMENT
  audio
}

up() {
  pamixer -i $INCREMENT
  audio
}

mute() {
  pamixer -t
  audio
}

case "$1" in
  up) up ;;
  down) down ;;
  mute) mute ;;
esac
