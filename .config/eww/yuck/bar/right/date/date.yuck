(defwidget datebox []
  (box 
    :class "box"
    (button 
      :class "button date"
      ; :orientation "h"
      ; :space-evenly false
      ; :spacing 10
      :onclick `eww open --toggle calendar --screen 0`
      (label :text " ${date}")
    )
  )
)
