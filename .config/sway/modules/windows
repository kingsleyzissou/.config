# swayconfig:
# window management

# Window rules
for_window [window_role="pop-up"] floating enable
for_window [app_id="org.gnome.(Nautilus|Weather)"] floating enable
for_window [app_id="mpv"] {
  border none 
  floating enable
  sticky enable
}

# surfing 🏄
assign [app_id="brave-browser"] workspace number 1
# for_window [app_id="brave-browser"] move to workspace number 1

# assign [app_id="kitty"] 2
# don't want to assign because we want to be able 
# to open multiple terminals in multiple workspaces

# https://github.com/swaywm/sway/issues/4045#issuecomment-482823035
set $AUTO 0
for_window [app_id="kitty"] mark --add "type_$$AUTO" ; set $$AUTO 0
for_window [con_mark="type_0"] mark --toggle "type_0"
for_window [con_mark="type_1"] mark --toggle "type_1"; move to workspace number 2; focus
for_window [con_mark="type_2"] mark --toggle "type_2"; move to workspace number 4; focus

# set chat
assign [class="Ferdium"] workspace number 3
# for_window [class="Ferdium"] move to workspace number 3

# set music - spotify is a bit stubborn
assign [class="Spotify"] workspace number 5
for_window [class="Spotify"] move to workspace number 5

# set password manager
assign [class="1Password"] workspace number 6

# Autostart programs (after assignment - duh!)
exec brave-browser &
exec kitty &
exec spotify &
exec 1password &
exec ferdium \
  --auth-server-whitelist '*.redhat.com' \
  --auth-negotiate-delegate-whitelist '*.redhat.com' &
